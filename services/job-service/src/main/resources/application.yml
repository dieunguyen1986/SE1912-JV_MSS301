server:
  port: 8081
spring:
  application:
    name: job-service
  datasource:
    url: jdbc:postgresql://localhost:5432/talenthub_job_db2
    username: dieunt
    password:
  jpa:
    hibernate:
      ddl-auto: update    # dev only, prod đổi qua Flyway
    properties.hibernate.format_sql: true
management:
  endpoints:
    web:
      exposure:
        include: health, info
