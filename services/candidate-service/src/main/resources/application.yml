server:
  port: 8082
spring:
  application:
    name: candidate-service
  datasource:
    url: jdbc:postgresql://localhost:5432/talenthub_candidate
    username: dieunt
    password: 
  jpa:
    hibernate:
      ddl-auto: update    # dev only, prod đổi qua Flyway
    properties.hibernate.format_sql: true
management:
  endpoints:
    web:
      exposure:
        include: health, info
